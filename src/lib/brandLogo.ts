/** Full wordmark (icon + SECUREVAULT + tagline) served from /public. */
export const BRAND_LOGO_SRC = '/brand/securevault-logo.png';

/** Intrinsic dimensions for Next/Image layout (approximate aspect of the PNG). */
export const BRAND_LOGO_WIDTH = 512;
export const BRAND_LOGO_HEIGHT = 640;

/** Primary coral and teal from brand artwork (works on light or dark UI). */
export const BRAND_CORAL = '#e85d4c';
export const BRAND_TEAL = '#4eb8b4';
